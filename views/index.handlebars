<div class="jumbotron">
  <h1 class="display-4">Bem vindo ao Node.Js BlogAPP</h1>
  <p class="lead"<p>Blog criado utilizando Javascrips, Node.JS, Express.JS, Express-Handlebars, Body-Parser e Mongoose.</p>
  <hr class="my-4">
  <p>Banco de dados utilizado neste blog foi o MongoBD.</p>
 
  <p class="lead">
    <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie uma conta</a>
  </p>
</div>
<hr>
<h2>Postagens recentes: </h2> 
{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">       
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small>
        <small>Data de publicação: {{data}}</small>
    </div>
</div>
{{else}}
    <h5>Nenhuma postagem</h5>
{{/each}}